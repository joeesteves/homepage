name: Build and Push to Docker Hub

on:
  push:
    branches:
      - main # Change this if your default branch is different
  workflow_dispatch: # Allows manual triggering

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set image tag
        id: image
        run: echo "tag=${GITHUB_SHA::12}" >> "$GITHUB_OUTPUT"

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          push: true
          build-args: COMMIT_SHA=${{ github.sha }}
          tags: |
            ${{ secrets.DOCKER_HUB_USERNAME }}/homepage:${{ steps.image.outputs.tag }}
            ${{ secrets.DOCKER_HUB_USERNAME }}/homepage:latest

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: "latest"

      - name: Configure Kubeconfig
        env:
          KUBECONFIG_CONTENT: ${{ secrets.KUBECONFIG }}
        run: |
          mkdir -p ~/.kube
          if ! printf '%s' "$KUBECONFIG_CONTENT" | base64 --decode > ~/.kube/config 2>/dev/null; then
            printf '%s' "$KUBECONFIG_CONTENT" > ~/.kube/config
          fi
          chmod 600 ~/.kube/config

      - name: Deploy commit image
        env:
          IMAGE: ${{ secrets.DOCKER_HUB_USERNAME }}/homepage:${{ steps.image.outputs.tag }}
        run: |
          kubectl -n esteves set image deployment/homepage "homepage=$IMAGE"
          kubectl -n esteves rollout status deployment/homepage --timeout=120s
